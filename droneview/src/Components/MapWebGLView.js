// import { Loader } from '@googlemaps/js-api-loader';

// const apiOptions = {
//   "apiKey": process.env.REACT_APP_GOOGLEMAP_API_KEY ,
//   "version": "beta"
// };

// const mapOptions = {
//   "zoom": 18,
//   "center": { lat: -1.091182, lng: 37.023082 },
//   "mapId": process.env.REACT_APP_GOOGLEMAP_ID,
// }

// async function initMap({mapRef}) {    
  
//   const apiLoader = new Loader(apiOptions);
//   await apiLoader.load()      
//   return new window.google.maps.Map(mapRef.current, mapOptions);
// }

// function initWebGLOverlayView (map) {
//   let scene, renderer, camera, loader;
//   // WebGLOverlayView code goes here
// }

// const MapView = (async ({mapRef}) => {        
//   const map = await initMap({mapRef});
// })();

// export default MapView;

